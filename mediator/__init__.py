"""Mediator pattern — a runnable tutorial package.

A login dialog whose widgets never talk to each other: every state change
is routed through a single mediator that decides which widgets are
enabled. Run the demo from the repository root:

    python -m mediator.main
"""
