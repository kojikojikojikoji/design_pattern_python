"""Memento pattern — a runnable tutorial package.

A tiny text editor whose full state can be snapshotted into opaque
Memento objects and restored later, giving undo without ever exposing
the editor's internals. Run the demo from the repository root:

    python -m memento.main
"""
