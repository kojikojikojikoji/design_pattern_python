"""State pattern — a runnable tutorial package.

A building security system whose reaction to the same events (safe
usage, alarms, phone calls) changes between day and night — not through
if-else ladders, but by swapping the State object that handles them.
Run the demo from the repository root:

    python -m state.main
"""
