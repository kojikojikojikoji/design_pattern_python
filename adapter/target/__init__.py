"""The Target side of the Adapter pattern.

This sub-package defines the interface the application *wants* to use
(:class:`~adapter.target.notifier.Notifier`). It knows nothing about the
legacy service being adapted.
"""
