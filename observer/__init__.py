"""Observer pattern — a runnable tutorial package.

A stock-price feed (Subject) broadcasts deterministic price ticks to
displays and alerts (Observers) that can subscribe and unsubscribe at
runtime — even in the middle of a notification. Run the demo from the
repository root:

    python -m observer.main
"""
